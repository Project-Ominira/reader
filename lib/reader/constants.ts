// Presence/collaboration ("N reading now", who else is here) is explicitly
// deferred in spec.md ("Read together") — kept as a display-only constant
// until that feature has a real data source.
export const READING_NOW_PLACEHOLDER = 4;
